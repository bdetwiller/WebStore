<h1> Login: </h1>

<%= form_for(@user, url: login_path) do |f| %>

  <%= f.label(:email) %>
  <%= f.text_field(:email)%>
  <br>
  <%= f.label(:password) %>
  <%= f.password_field(:password) %>
  <br>
  <%= f.submit(:value => "Login", class: "btn btn-large") %>
<% end %>